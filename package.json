{
  "name": "real-estate-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}