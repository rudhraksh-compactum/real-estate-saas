---
phase: "01-infrastructure-setup"
plan: "1.2"
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - payload.config.ts
  - packages/shared/src/types.ts
  - packages/shared/package.json
  - tsconfig.json
  - .vscode/launch.json
  - Dockerfile
autonomous: true
requirements:
  - MULTI-04
  - PROP-02
must_haves:
  truths:
    - "pnpm workspace includes payload/, frontend/, packages/ directories"
    - "Payload config uses @payloadcms/next plugin and connects to PostgreSQL"
    - "Stub collections exist for Tenants and Properties (Phase 2 full implementation)"
    - "VS Code launch.json has debug configs for Payload and Next.js"
    - "TypeScript monorepo structure with proper path aliases"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace definition"
      min_lines: 5
    - path: "payload.config.ts"
      provides: "Payload CMS configuration with embedded Next.js"
      min_lines: 50
    - path: "packages/shared/src/types.ts"
      provides: "Shared TypeScript types for Tenant and Property"
      min_lines: 20
    - path: ".vscode/launch.json"
      provides: "VS Code debug configurations"
      min_lines: 30
    - path: "Dockerfile"
      provides: "Multi-stage build for Node.js app"
      min_lines: 20
  key_links:
    - from: "payload.config.ts"
      to: "@payloadcms/next"
      via: "import and plugin registration"
      pattern: "@payloadcms/next"
    - from: "payload.config.ts"
      to: "@payloadcms/db-postgres"
      via: "import and db adapter"
      pattern: "db-postgres"
    - from: "packages/shared"
      to: "frontend/, payload/"
      via: "TypeScript project references"
      pattern: "path"
---

<objective>
Configure Payload CMS with monorepo structure, PostgreSQL connection, stub collections, shared types, and VS Code debugging support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Per D-01: Payload CMS embedded inside Next.js via @payloadcms/next plugin
Per D-02: pnpm workspaces monorepo structure with payload/, frontend/, packages/
Per D-03: Workspace packages for shared utilities (packages/shared/)
Per D-06: VS Code debugging config (launch.json)
Per D-11: Environment variables for Payload DB connection
Per D-12: payload.config.ts in root
Per D-13: Standard pnpm workspace config (pnpm-workspace.yaml)

From research/STACK.md:
- Payload 3.x supports PostgreSQL via @payloadcms/db-postgres
- Next.js 14+ with App Router requires Node 18+

From research/ARCHITECTURE.md:
- Recommended structure: payload/src/collections/ with field composition pattern
- Properties extension: base fields + vertical-specific files
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create pnpm-workspace.yaml and root package.json</name>
  <files>pnpm-workspace.yaml, package.json</files>
  <action>
    Create pnpm-workspace.yaml at project root:
    ```yaml
    packages:
      - 'payload'
      - 'frontend'
      - 'packages/*'
    ```

    Create package.json at project root:
    ```json
    {
      "name": "real-estate-saas",
      "version": "1.0.0",
      "private": true,
      "scripts": {
        "dev": "pnpm --filter frontend dev",
        "build": "pnpm --filter frontend build",
        "payload": "pnpm --filter payload dev",
        "clean": "rm -rf node_modules .next packages/*/node_modules payload/node_modules frontend/node_modules"
      },
      "engines": {
        "node": ">=18.0.0",
        "pnpm": ">=8.0.0"
      }
    }
    ```
  </action>
  <verify>
    <automated>cat pnpm-workspace.yaml && cat package.json</automated>
  </verify>
  <done>
    pnpm-workspace.yaml exists with packages: payload, frontend, packages/* entries. package.json has name, version, scripts for dev/build/clean
  </done>
</task>

<task type="execute">
  <name>Task 2: Create payload.config.ts with @payloadcms/next plugin and PostgreSQL adapter</name>
  <files>payload.config.ts</files>
  <action>
    Create payload.config.ts at project root with the following structure:

    1. **Imports:**
       - `import { buildConfig } from 'payload'`
       - `import { nextPlugin } from '@payloadcms/next'`
       - `import { postgresAdapter } from '@payloadcms/db-postgres'`
       - `import { TenantsStub } from './payload/src/collections/TenantsStub'`
       - `import { PropertiesStub } from './payload/src/collections/PropertiesStub'`

    2. **Export buildConfig with:**
       ```typescript
       export default buildConfig({
         admin: {
           user: 'users', // Reference to users collection
           meta: {
             titleSuffix: '- Real Estate SaaS',
             favicon: '/favicon.ico',
           },
         },
         plugins: [
           nextPlugin({
             // Point to Next.js config
             nextConfig: './next.config.ts',
           }),
         ],
         db: postgresAdapter({
           // Use DATABASE_URL env var
           // Connection pool config for production
         }),
         collections: [
           // Stub collections (Phase 2 full implementation)
           // See task 3 for structure
         ],
         secret: process.env.PAYLOAD_SECRET,
         types: {
           // Generate types from collections
           autofill: true,
         },
       });
       ```

    3. **Create payload/src/collections/TenantsStub.ts:**
       ```typescript
       import { CollectionConfig } from 'payload';

       export const TenantsStub: CollectionConfig = {
         slug: 'tenants',
         fields: [
           { name: 'name', type: 'text', required: true },
           { name: 'slug', type: 'text', required: true, unique: true },
           {
             name: 'verticalType',
             type: 'select',
             options: [
               { label: 'Airbnb/Short-Let', value: 'airbnb' },
               { label: 'Real Estate Agent', value: 'agent' },
               { label: 'Property Builder', value: 'builder' },
             ],
             required: true,
           },
           // Branding fields (Phase 2)
           // { name: 'logo', type: 'upload', relationTo: 'media' },
           // { name: 'primaryColor', type: 'text' },
           // { name: 'tagline', type: 'text' },
         ],
         // Access: tenant admins only (Phase 2)
       };
       ```

    4. **Create payload/src/collections/PropertiesStub.ts:**
       ```typescript
       import { CollectionConfig } from 'payload';

       export const PropertiesStub: CollectionConfig = {
         slug: 'properties',
         fields: [
           { name: 'title', type: 'text', required: true },
           { name: 'description', type: 'text' },
           {
             name: 'address',
             type: 'group',
             fields: [
               { name: 'street', type: 'text' },
               { name: 'city', type: 'text' },
               { name: 'state', type: 'text' },
               { name: 'zipCode', type: 'text' },
               { name: 'country', type: 'text' },
             ],
           },
           {
             name: 'geolocation',
             type: 'point', // PostGIS geometry point
           },
           // Stub fields (Phase 2):
           // BHK type, furnishing status, bathrooms, floor, etc.
           // Agent extension: rentPrice, salePrice, listingMode
           // Airbnb extension: nightlyPricing, amenities, houseRules
         ],
         // Access: tenant-scoped (Phase 2)
         // Index: GIST on geolocation (Phase 2)
       };
       ```

    Note: These are STUBS only per phase scope. Full collections come in Phase 2.
  </action>
  <verify>
    <automated>grep -c "@payloadcms/next" payload.config.ts && grep -c "@payloadcms/db-postgres" payload.config.ts && grep -c "tenants" payload.config.ts && grep -c "properties" payload.config.ts</automated>
  </verify>
  <done>
    payload.config.ts exists with @payloadcms/next plugin, postgresAdapter, TenantsStub, PropertiesStub collections. Collections are stubs only (full implementation Phase 2).
  </done>
</task>

<task type="execute">
  <name>Task 3: Create shared types package for Tenant and Property</name>
  <files>packages/shared/src/types.ts, packages/shared/package.json, packages/shared/tsconfig.json</files>
  <action>
    Create packages/shared/ directory structure:

    1. **packages/shared/package.json:**
       ```json
       {
         "name": "@real-estate-saas/shared",
         "version": "1.0.0",
         "private": true,
         "main": "./src/index.ts",
         "types": "./src/index.ts",
         "exports": {
           ".": "./src/index.ts"
         }
       }
       ```

    2. **packages/shared/tsconfig.json:**
       ```json
       {
         "compilerOptions": {
           "target": "ES2020",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "declaration": true,
           "strict": true,
           "outDir": "./dist",
           "rootDir": "./src"
         },
         "include": ["src/**/*"],
         "exclude": ["node_modules", "dist"]
       }
       ```

    3. **packages/shared/src/types.ts:**
       ```typescript
       // Tenant types (MULTI-04 stub)
       export type VerticalType = 'airbnb' | 'agent' | 'builder';

       export interface Tenant {
         id: string;
         name: string;
         slug: string;
         verticalType: VerticalType;
         // Branding fields (Phase 2): logo, primaryColor, tagline
       }

       // Property types (PROP-02 stub)
       export interface Address {
         street: string;
         city: string;
         state: string;
         zipCode: string;
         country: string;
       }

       export interface Geolocation {
         lat: number;
         lng: number;
       }

       export interface Property {
         id: string;
         title: string;
         description: string;
         address: Address;
         geolocation: Geolocation;
         // Stub fields (Phase 2):
         // BHK type, furnishing, bathrooms, floor, etc.
         // Agent extension: listingMode, rentPrice, salePrice
         // Airbnb extension: nightlyPricing, amenities, houseRules
       }

       // Export all types
       export type { Tenant, Property, Address, Geolocation, VerticalType };
       ```

    4. **packages/shared/src/index.ts:**
       ```typescript
       export * from './types';
       ```
  </action>
  <verify>
    <automated>grep -c "VerticalType" packages/shared/src/types.ts && grep -c "Property" packages/shared/src/types.ts && grep -c "Geolocation" packages/shared/src/types.ts</automated>
  </verify>
  <done>
    packages/shared/src/types.ts exists with Tenant, Property, Address, Geolocation, VerticalType types. package.json and tsconfig.json configured for workspace usage.
  </done>
</task>

<task type="execute">
  <name>Task 4: Create root tsconfig.json for monorepo</name>
  <files>tsconfig.json</files>
  <action>
    Create tsconfig.json at project root:
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "module": "ESNext",
        "lib": ["ES2020"],
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "allowJs": true,
        "checkJs": false,
        "jsx": "react-jsx",
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "outDir": "./dist",
        "rootDir": ".",
        "composite": true,
        "incremental": true,
        "skipLibCheck": true,
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "forceConsistentCasingInFileNames": true,
        "esModuleInterop": true,
        "allowSyntheticDefaultImports": true,
        "paths": {
          "@/*": ["./*"],
          "@payload/*": ["./payload/*"],
          "@frontend/*": ["./frontend/*"],
          "@shared/*": ["./packages/shared/*"]
        }
      },
      "include": [
        "**/*.ts",
        "**/*.tsx",
        "**/.js",
        "**/.jsx",
        "**/.json"
      ],
      "exclude": [
        "node_modules",
        ".next",
        "dist",
        "**/dist",
        "**/build",
        "payload/dist",
        "frontend/dist",
        "frontend/.next"
      ],
      "references": [
        { "path": "./packages/shared" },
        { "path": "./frontend" },
        { "path": "./payload" }
      ]
    }
    ```
  </action>
  <verify>
    <automated>grep -c "composite" tsconfig.json && grep -c "references" tsconfig.json && grep -c "@shared" tsconfig.json</automated>
  </verify>
  <done>
    tsconfig.json exists with composite: true, references to shared/frontend/payload, and path aliases for workspace packages
  </done>
</task>

<task type="execute">
  <name>Task 5: Create VS Code launch.json for debugging</name>
  <files>.vscode/launch.json</files>
  <action>
    Create .vscode/launch.json with the following configurations:

    1. **Node.js Attach (Payload):**
       ```json
       {
         "name": "Payload: Attach to Node",
         "type": "node",
         "request": "attach",
         "port": 9229,
         "restart": true,
         "skipFiles": ["<node_internals>/**"],
         "env": {
           "NODE_ENV": "development"
         }
       }
       ```

    2. **Next.js: Debug with Chrome/Edge:**
       ```json
       {
         "name": "Next.js: Chrome/Edge",
         "type": "chrome",
         "request": "launch",
         "url": "http://localhost:3000",
         "webRoot": "${workspaceFolder}/frontend",
         "skipFiles": ["<node_internals>/**"]
       }
       ```

    3. **Next.js: Node.js Inspector:**
       ```json
       {
         "name": "Next.js: Node Inspector",
         "type": "node",
         "request": "launch",
         "runtimeExecutable": "pnpm",
         "runtimeArgs": ["dev"],
         "cwd": "${workspaceFolder}/frontend",
         "skipFiles": ["<node_internals>/**"],
         "env": {
           "NODE_OPTIONS": "--inspect=9228"
         }
       }
       ```

    4. **Compound: Full Debug:**
       ```json
       {
         "name": "Debug All (Payload + Next.js)",
         "compounds": [
           {
             "name": "Debug All (Payload + Next.js)",
             "configurations": [
               "Payload: Attach to Node",
               "Next.js: Chrome/Edge"
             ]
           }
         ]
       }
       ```

    Also create .vscode/settings.json:
    ```json
    {
      "typescript.tsdk": "node_modules/typescript/lib",
      "editor.formatOnSave": true,
      "editor.defaultFormatter": "esbenp.prettier-vscode",
      "editor.codeActionsOnSave": {
        "source.fixAll.eslint": "explicit"
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "Payload" .vscode/launch.json && grep -c "Next.js" .vscode/launch.json && grep -c "compounds" .vscode/launch.json</automated>
  </verify>
  <done>
    .vscode/launch.json exists with Payload attach, Next.js Chrome, Next.js Node Inspector configs, and compound Debug All configuration
  </done>
</task>

<task type="execute">
  <name>Task 6: Create Dockerfile for multi-stage Node.js build</name>
  <files>Dockerfile</files>
  <action>
    Create Dockerfile at project root with multi-stage build:

    ```dockerfile
    # Stage 1: Dependencies
    FROM node:18-alpine AS deps
    RUN apk add --no-cache libc6-compat
    WORKDIR /app

    # Install pnpm globally
    RUN npm install -g pnpm

    COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
    COPY packages/shared/package.json ./packages/shared/
    # Frontend and payload package.json will be added in their respective phases

    RUN pnpm install --frozen-lockfile

    # Stage 2: Build
    FROM node:18-alpine AS builder
    WORKDIR /app

    COPY --from=deps /app/node_modules ./node_modules
    COPY . .

    # Build Next.js (payload config embedded)
    RUN pnpm --filter frontend build || echo "Frontend build deferred to Phase 1.3"

    # Stage 3: Runner
    FROM node:18-alpine AS runner
    WORKDIR /app

    ENV NODE_ENV=production

    RUN addgroup --system --gid 1001 nodejs
    RUN adduser --system --uid 1001 nextjs

    COPY --from=builder /app/public ./public
    COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
    COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

    USER nextjs

    EXPOSE 3000

    ENV PORT=3000
    ENV HOSTNAME="0.0.0.0"

    CMD ["node", "server.js"]
    ```

    Note: Frontend build command may fail until Plan 1.3 creates the frontend package.json. This is expected for Phase 1.
  </action>
  <verify>
    <automated>grep -c "FROM node:18" Dockerfile && grep -c "pnpm" Dockerfile && grep -c "EXPOSE 3000" Dockerfile</automated>
  </verify>
  <done>
    Dockerfile exists with multi-stage build (deps, builder, runner), pnpm installation, Next.js build step, and proper user setup
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> postgres | Database connection via TCP with credentials |
| dev -> prod secrets | PAYLOAD_SECRET in env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Disclosure | PAYLOAD_SECRET | mitigate | Loaded from env var, never committed to repo |
| T-01-04 | Tampering | pnpm-workspace | accept | Standard pnpm workspace, no attack surface |
| T-01-05 | Elevation | Docker USER | mitigate | Use non-root user (nextjs) in final stage |
</threat_model>

<verification>
After all plans execute:
1. `pnpm install` succeeds and workspace packages are linked
2. `pnpm list` shows all three packages installed
3. `grep -r "from '@payloadcms" payload.config.ts` confirms correct imports
4. VS Code "Debug All" compound appears in debug configuration dropdown
5. `docker build` produces no errors (frontend build may be deferred)
</verification>

<success_criteria>
1. pnpm-workspace.yaml includes payload/, frontend/, packages/*
2. payload.config.ts uses @payloadcms/next plugin
3. payload.config.ts uses @payloadcms/db-postgres adapter
4. TenantsStub collection exists with slug, name, verticalType fields
5. PropertiesStub collection exists with title, description, address, geolocation fields
6. packages/shared has Tenant, Property, Address, Geolocation types
7. tsconfig.json has composite: true and project references
8. .vscode/launch.json has Payload attach and Next.js debug configs
9. Dockerfile exists with multi-stage build
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-setup/1.2-SUMMARY.md` when done
</output>
