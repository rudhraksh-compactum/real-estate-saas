---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - payload/src/collections/PropertiesStub.ts
autonomous: true
requirements:
  - REQ-6.1
  - REQ-6.2
  - REQ-6.3
must_haves:
  truths:
    - "All field labels are user-friendly (not technical jargon)"
    - "Complex fields have helpful descriptions with examples"
    - "Required fields are clearly marked"
    - "Field ordering follows a logical workflow"
  artifacts:
    - path: payload/src/collections/PropertiesStub.ts
      provides: Optimized property edit form
      exports: PropertiesStub collection config
---

<objective>
Polish the Properties collection admin form for non-technical users. Focus on friendly labels, helpful descriptions, and logical field organization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@payload/src/collections/PropertiesStub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and enhance field labels</name>
  <files>payload/src/collections/PropertiesStub.ts</files>
  <read_first>
    payload/src/collections/PropertiesStub.ts
  </read_first>
  <action>
Review and enhance the Properties collection with user-friendly labels:

**A) Basic Info Tab:**
- `title` - Keep as-is, it's already clear
- `description` - Already good
- `shortDescription` - Add placeholder: "A cozy 2-bedroom apartment near the beach"

**B) Location Tab:**
- Add `admin.label` overrides for friendly names:
  - `geolocation` → label: "Map Location"
  - `locality` → label: "Neighborhood/Area"

**C) Property Details Tab:**
- Add label overrides:
  - `bhkType` → label: "Bedroom Configuration"
  - `propertyType` → label: "Type of Property"
  - `furnishingStatus` → label: "Furnishing Level"
  - `floor.current` → label: "Floor Number"
  - `floor.total` → label: "Total Floors in Building"
  - `facing` → label: "Direction Facing"

**D) Pricing Tab:**
- Enhance descriptions:
  - `nightlyPrice` → Add example: "e.g., 2500 for ₹2,500 per night"
  - `minNights` → Add example: "e.g., 2 for minimum 2 nights stay"
  - `maxNights` → Add placeholder: "Leave empty for no limit"

**E) Amenities & Rules Tab:**
- `amenities` → label: "What Amenities Are Available?"
- `houseRules` → Add placeholder with examples
- `petPolicy` → label: "Pet Policy"
  - Update options to be more friendly:
    - "Allowed" instead of "pets_allowed"
    - "Not Allowed" instead of "pets_not_allowed"
    - "Cats Only" → "Cats Welcome"
    - "Dogs Only" → "Dogs Welcome"
- `tenantPreference` → label: "Best Suited For"
  - Update options:
    - "Families" → "Families with Kids"
    - "Business Travelers" → "Business Travelers"

**F) Media Tab:**
- `gallery` → label: "Property Photos"
- `featuredImage` → label: "Cover Photo (Main Listing Image)"
  - Enhance description: "This image will be shown as the main photo in search results and listings"

**G) Sidebar:**
- `status` → Add description: "Draft = not visible to guests, Published = live on website"
</action>
  <verify>
    <automated>grep -c "label:" payload/src/collections/PropertiesStub.ts && grep -c "placeholder:" payload/src/collections/PropertiesStub.ts</automated>
  </verify>
  <done>Field labels enhanced with user-friendly names, placeholders added where helpful</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional field visibility</name>
  <files>payload/src/collections/PropertiesStub.ts</files>
  <action>
Add conditional field visibility using Payload's `admin.condition`:

**A) Seasonal pricing fields:**
- Only show `seasonalPricing` array if there's meaningful content (always visible for now)

**B) Floor fields:**
- Only show `floor.current` and `floor.total` if apartment/condo selected

**C) Facing field:**
- Could be hidden for certain property types (optional - skip if complex)

Note: Payload 3.x supports conditions. Example pattern:
```typescript
admin: {
  condition: (data) => data.propertyType !== 'villa',
}
```

Keep it simple - just ensure fields are well-labeled, don't over-engineer conditions.
</action>
  <verify>
    <automated>grep -c "condition:" payload/src/collections/PropertiesStub.ts</automated>
  </verify>
  <done>Conditional field visibility added where appropriate</done>
</task>

<task type="auto">
  <name>Task 3: Verify admin meta configuration</name>
  <files>payload.config.ts</files>
  <read_first>
    payload.config.ts
  </read_first>
  <action>
Check payload.config.ts admin settings and enhance if needed:

1. Verify admin meta title suffix is set appropriately
2. Consider adding admin welcome banner or custom CSS for branding

The current config has:
```typescript
admin: {
  user: 'users',
  meta: {
    titleSuffix: '- Real Estate SaaS',
  },
},
```

Update titleSuffix to be more friendly: "Not Just A Stay - Admin"

Note: This affects all collections, so make it generic but polished.
</action>
  <verify>
    <automated>grep "titleSuffix" payload.config.ts</automated>
  </verify>
  <done>Admin meta configuration verified and enhanced</done>
</task>

</tasks>

<verification>
```bash
# Verify changes made
grep -n "label:" payload/src/collections/PropertiesStub.ts | head -20
grep -n "placeholder:" payload/src/collections/PropertiesStub.ts | head -10
grep -n "admin.description" payload/src/collections/PropertiesStub.ts | head -10

# TypeScript check
cd payload && npx tsc --noEmit
```
</verification>

<success_criteria>
1. All field labels are user-friendly and consistent
2. Required fields have clear indication (required: true already set)
3. Placeholder text provides examples where helpful
4. Field descriptions are enhanced with examples
5. Pet policy and tenant preference options are friendly
</success_criteria>

<output>
Create `.planning/phases/06-admin-polish/6.1-SUMMARY.md` when done
</output>
