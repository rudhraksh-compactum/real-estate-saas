---
phase: "06"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - payload/src/collections/Activities.ts
  - payload/src/collections/Media.ts
autonomous: true
requirements:
  - REQ-6.4
  - REQ-6.5
must_haves:
  truths:
    - "Activity fields have friendly labels and descriptions"
    - "Media upload has clear guidance"
    - "Image preview is clear and intuitive"
    - "Form flow is logical for non-technical users"
  artifacts:
    - path: payload/src/collections/Activities.ts
      provides: Optimized activity edit form
      exports: Activities collection config
    - path: payload/src/collections/Media.ts
      provides: Media collection with upload guidance
      exports: Media collection config
---

<objective>
Polish the Activities collection and Media library for non-technical users. Focus on friendly labels, upload guidance, and clear image previews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@payload/src/collections/Activities.ts
@payload/src/collections/Media.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Activity fields</name>
  <files>payload/src/collections/Activities.ts</files>
  <read_first>
    payload/src/collections/Activities.ts
  </read_first>
  <action>
Enhance the Activities collection with user-friendly labels:

**A) Basic Info Tab:**
- `title` - Add placeholder: "Sunset Beach Yoga Session"
- `shortDescription` - Add placeholder: "A relaxing yoga experience as the sun goes down"

**B) Details Tab:**
- `description` - Add placeholder with example structure
- `highlights` - Enhance description: "Key selling points guests will love (e.g., 'Learn authentic recipes', 'Stunning views')"
- `duration` - Add placeholder: "e.g., 2 hours, Half day (4 hours), Full day (8 hours)"
- `groupSize.minGuests` → label: "Minimum Guests"
- `groupSize.maxGuests` → label: "Maximum Guests"

**C) Pricing Tab:**
- `price` - Add description with example: "Price per person, e.g., 1500 for ₹1,500 per person"
- `includes` - Add placeholder: "e.g., All ingredients, Recipe booklet, Take-home samples"
- `cancellationPolicy` - Add placeholder with example structure

**D) Media Tab:**
- `gallery` → label: "Experience Photos"
- `featuredImage` → label: "Cover Photo"
  - Add description: "This image will be shown in search results and listings"
- `gallery` - Add description: "Drag photos to reorder. First photo is shown as thumbnail."

**E) Availability Tab:**
- `bookingLeadTime` → label: "Advance Notice Required"
  - Update description: "Minimum hours between booking and experience start"

**F) Sidebar:**
- `status` - Add description: "Draft = not visible, Published = live on website"
- `linkedProperties` → label: "Related Properties"
  - Add description: "Link this experience to relevant properties for bundled bookings"
</action>
  <verify>
    <automated>grep -c "label:" payload/src/collections/Activities.ts && grep -c "placeholder:" payload/src/collections/Activities.ts</automated>
  </verify>
  <done>Activity fields polished with friendly labels and placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Media collection</name>
  <files>payload/src/collections/Media.ts</files>
  <read_first>
    payload/src/collections/Media.ts
  </read_first>
  <action>
Review and enhance the Media collection for better upload experience:

1. Check if Media collection has proper admin configuration
2. Add friendly labels and descriptions
3. Ensure image preview is configured

Current Media collection likely uses Payload's built-in media type. Enhance if custom.

**A) Add upload guidance:**
- Create a custom admin component or use description for guidance
- Recommended: Add collection description in admin.meta

**B) If custom Media collection exists:**
- Add field labels:
  - `filename` → label: "File Name"
  - `alt` → label: "Alt Text" with description: "Describe the image for screen readers and SEO"
  - `caption` → label: "Caption" with description: "Optional caption displayed with the image"

**C) Collection-level admin:**
```typescript
admin: {
  description: 'Images and files for your website',
  components: {
    // Payload 3.x allows custom components
  },
},
```
</action>
  <verify>
    <automated>ls payload/src/collections/Media.ts && head -50 payload/src/collections/Media.ts</automated>
  </verify>
  <done>Media collection reviewed and enhanced with upload guidance</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and documentation</name>
  <files>
    payload/src/collections/Activities.ts
    payload/src/collections/Media.ts
  </files>
  <action>
Final verification:

1. Run TypeScript check on modified files
2. Verify no breaking changes to field slugs (slugs stay the same, only labels/descriptions change)
3. Document any API changes (there should be none - admin polish doesn't affect API)

Summary of changes:
- All field labels are now user-friendly
- Placeholders provide examples
- Descriptions enhanced with context
- No breaking changes to data model
</action>
  <verify>
    <automated>cd payload && npx tsc --noEmit</automated>
  </verify>
  <done>Final verification complete, no breaking changes</done>
</task>

</tasks>

<verification>
```bash
# Verify changes made
grep -n "label:" payload/src/collections/Activities.ts | head -15
grep -n "placeholder:" payload/src/collections/Activities.ts | head -10

# Check Media collection exists
ls payload/src/collections/Media.ts

# TypeScript check
cd payload && npx tsc --noEmit
```
</verification>

<success_criteria>
1. Activity fields have friendly labels and descriptions
2. Media upload has clear guidance
3. Image preview is clear and intuitive
4. Form flow is logical for non-technical users
5. No breaking changes to API
</success_criteria>

<output>
Create `.planning/phases/06-admin-polish/6.2-SUMMARY.md` when done
</output>
