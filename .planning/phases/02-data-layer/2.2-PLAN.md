---
phase: "2"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - payload/src/collections/Leads.ts
autonomous: true
requirements:
  - REQ-P2-02
must_haves:
  truths:
    - "Public users can submit leads via inquiry form"
    - "Admin can view and manage leads"
    - "Leads are linked to properties"
  artifacts:
    - path: payload/src/collections/Leads.ts
      provides: Leads collection for inquiry capture
      min_lines: 100
      exports: Leads (CollectionConfig)
  key_links:
    - from: Leads.ts
      to: Properties collection
      via: relationship field
      pattern: relationTo: 'properties'
---

<objective>
Create a Leads collection for capturing inquiry data from the property inquiry form. The collection allows public create access (for website visitors) while restricting read/update/delete to authenticated admin users. Leads are linked to properties for context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@payload.config.ts

## Pattern from RESEARCH.md

```typescript
const Leads: CollectionConfig = {
  slug: 'leads',
  access: {
    create: () => true,  // Public can create leads
    read: ({ req: { user } }) => Boolean(user),
    update: ({ req: { user } }) => Boolean(user),
    delete: ({ req: { user } }) => Boolean(user),
  },
  fields: [
    { name: 'name', type: 'text', required: true },
    { name: 'email', type: 'email', required: true },
    { name: 'phone', type: 'text' },
    { name: 'message', type: 'textarea' },
    { name: 'propertyReference', type: 'relationship', relationTo: 'properties' },
    { name: 'status', type: 'select', defaultValue: 'new' },
  ],
}
```

## Decisions from CONTEXT.md
- D-09: Leads collection for inquiry capture
- D-12: Owner role with full access
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Leads collection</name>
  <files>payload/src/collections/Leads.ts</files>
  <read_first>payload.config.ts</read_first>
  <action>
    Create a new file payload/src/collections/Leads.ts with a Payload CMS collection for capturing inquiry leads.

    The collection MUST include:

    1. **Admin configuration:**
       - `useAsTitle: 'name'`
       - `defaultColumns: ['name', 'email', 'propertyReference', 'status', 'createdAt']`

    2. **Access control:**
       - `create: () => true` - Public can submit leads (inquiry form)
       - `read: ({ req: { user } }) => Boolean(user)` - Admin only
       - `update: ({ req: { user } }) => Boolean(user)` - Admin only
       - `delete: ({ req: { user } }) => Boolean(user)` - Admin only

    3. **Fields:**
       - name (text, required) - admin description: 'Guest or lead name'
       - email (email, required)
       - phone (text) - admin description: 'Phone number with country code'
       - message (textarea) - admin description: 'Inquiry message from guest'
       - propertyReference (relationship, relationTo: 'properties') - admin description: 'Property this lead inquired about'
       - status (select, defaultValue: 'new', required, position: 'sidebar'):
         - New, Contacted, Qualified, Converted, Lost
       - notes (textarea) - admin description: 'Internal notes about this lead'
       - source (select):
         - Property Inquiry, Contact Form, Activity Inquiry (Phase 3)

    4. **Hooks (afterChange):**
       - Include placeholder hook for future email notification on new leads
       - Check if operation === 'create' and status === 'new' for future email logic

    Export the collection as `Leads`.
  </action>
  <verify>
    <automated>grep -c "slug: 'leads'" payload/src/collections/Leads.ts && grep -c "create: () => true" payload/src/collections/Leads.ts</automated>
  </verify>
  <acceptance_criteria>
    - CollectionConfig export present with slug: 'leads'
    - create: () => true (public create access)
    - read/update/delete restricted to authenticated users
    - name field is required text
    - email field is required email type
    - propertyReference relationship to 'properties' present
    - status select field with defaultValue: 'new'
    - status options: New, Contacted, Qualified, Converted, Lost
  </acceptance_criteria>
  <done>
    Leads.ts created with public create access, admin read/update/delete, all required fields (name, email, phone, message, propertyReference, status), and relationship to properties collection
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public -> API | Untrusted input crosses here (inquiry form) |
| Leads collection | Contains PII (email, phone) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-01 | Information Disclosure | Lead PII | mitigate | Access control restricts read to authenticated users only |
| T-2.2-02 | Spoofing | Fake lead submission | accept | Captcha (Phase 3 or external) will address, low risk for MVP |
| T-2.2-03 | Denial | Lead spam | accept | Volume is low for MVP, rate limiting in Phase 7 if needed |
| T-2.2-04 | Tampering | Status changes | mitigate | Access control restricts update to authenticated users |
</threat_model>

<verification>
1. File exists: `ls payload/src/collections/Leads.ts`
2. Contains public create access: `grep "create: () => true" payload/src/collections/Leads.ts`
3. Contains relationship: `grep "relationTo: 'properties'" payload/src/collections/Leads.ts`
4. TypeScript compiles: `cd payload && npx tsc --noEmit`
</verification>

<success_criteria>
- Leads.ts created with CollectionConfig export
- Public create access enabled (create: () => true)
- Admin-only read/update/delete access
- All required fields present: name, email, phone, message, propertyReference, status
- Relationship to properties collection configured
- Status workflow options: new, contacted, qualified, converted, lost
- TypeScript compiles without errors
</success_criteria>

<output>
Create `.planning/phases/02-data-layer/2.2-SUMMARY.md` when done
</output>
